# ==============================================================================
# Configuração para Produção na VPS Hostinger (ex: gettessera.io / tessera.data)
# ==============================================================================

server {
    listen 80;
    server_name gettessera.io www.gettessera.io;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name gettessera.io www.gettessera.io;

    ssl_certificate /etc/letsencrypt/live/gettessera.io/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/gettessera.io/privkey.pem;

    root /var/www/tessera;
    index index.html;

    # Segurança & Headers HTTP
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;
    add_header Content-Security-Policy "default-src 'self' https: data: 'unsafe-inline';" always;

    # Gzip / Compressão
    gzip on;
    gzip_comp_level 6;
    gzip_types text/plain text/css application/javascript application/json image/svg+xml;

    location / {
        try_files $uri $uri/ =404;
    }

    location ~* \.(?:css|js|svg|png|jpg|jpeg|ico|woff2?)$ {
        expires 365d;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }
}
