const fs = require('fs');
// Let's create an uncompressed BMP or raw PNG or we can use canvas/puppeteer or an HTML file rendered to PNG
// Actually, in modern web, OpenGraph supports SVG on many platforms, but let's check if we have any other tool like qlmanage, chromium, etc.
console.log('Checking available tools...');
